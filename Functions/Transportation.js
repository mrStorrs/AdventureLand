/* ----------------------------------------------------------------
 * This will eventually hold the logic for moving between areas / servers
 * for right now the scripts are simply using the in game function:
 * smart_move(). This function is inefficient as you have to wait 
 * some time for the character to path to where it needs to go. 
 * --------------------------------------------------------------*/